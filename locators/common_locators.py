from selenium.webdriver.common.by import By

class CommonLocators:
    # Example: If you had a universal header or footer search bar
    # SEARCH_BAR = (By.CSS_SELECTOR, "#universal-search")

    # You can add common elements like loading spinners if they have consistent locators
    # LOADING_SPINNER = (By.CSS_SELECTOR, ".global-spinner-class")
    pass